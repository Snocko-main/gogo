[package]
name = "actix-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
actix-rt = "2"
rusqlite = { version = "0.31", features = ["bundled"] }
rand = "0.8"
once_cell = "1"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
